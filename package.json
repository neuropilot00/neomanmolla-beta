{
  "name": "neomanmolla-beta",
  "version": "0.2.0",
  "private": true,
  "description": "Mobile social deduction beta with a lightweight Railway API.",
  "type": "commonjs",
  "scripts": {
    "start": "node server/index.js",
    "check": "node --check app.js && node --check game-data.js && node --check server/index.js"
  },
  "dependencies": {
    "pg": "^8.16.3"
  },
  "engines": {
    "node": ">=20"
  }
}
